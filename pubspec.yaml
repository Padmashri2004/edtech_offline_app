name: edtech_offline_app
description: "Offline EdTech portal with Gemma 3 AI and Image Library"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # AI & Persistence
  flutter_gemma: ^0.12.0
  sqflite: ^2.4.2
  path: ^1.9.1
  # File Management & PDF Processing (Mod 1 & 6)
  path_provider: ^2.1.5
  pdf_render: ^1.4.12
  crypto: ^3.0.7
<<<<<<< HEAD
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  logger: ^2.4.0
=======
  syncfusion_flutter_pdf: ^21.1.35  # Helps us read the PDF content
  file_picker: ^5.3.0
  logger: ^2.5.0
  provider: ^6.1.1
>>>>>>> f4a6942 (finalized offline AI engine, textbook parser, and quiz persistence)

dev_dependencies:
  flutter_test:
    sdk: flutter
  # FIXED: Moved from dependencies to dev_dependencies
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/gemma.task
    - assets/textbooks/